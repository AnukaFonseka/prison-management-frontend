import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Login - Prison Management System',
  description: 'Sign in to access the prison management system',
};

export default function LoginPage() {
  return <LoginForm />;
}